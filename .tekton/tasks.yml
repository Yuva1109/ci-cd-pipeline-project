apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: cleanup
spec:
  steps:
    - name: cleanup-step
      image: ubuntu
      script: |
        echo "Cleaning workspace..."

---
apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: nose-test
spec:
  steps:
    - name: test
      image: python
      script: |
        pip install nose
        nosetests
